services:
  drupaldev_default_content.export_command:
    class: Drupal\drupaldev_default_content\Command\ExportCommand
    arguments: ['@drupaldev_default_content.content_exporter', '@entity_type.manager', '@entity_type.bundle.info']
    tags:
      - { name: drupal.command }
