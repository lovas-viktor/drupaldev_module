langcode: en
status: true
dependencies:
  config:
    - image.style.cart
id: cart
label: Cart
image_style_mappings:
  -
    image_mapping_type: image_style
    image_mapping: cart
    breakpoint_id: responsive_image.viewport_sizing
    multiplier: 1x
breakpoint_group: responsive_image
fallback_image_style: cart
