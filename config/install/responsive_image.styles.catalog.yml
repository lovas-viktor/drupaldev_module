langcode: en
status: true
dependencies:
  config:
    - image.style.catalog
id: catalog
label: Catalog
image_style_mappings:
  -
    image_mapping_type: image_style
    image_mapping: catalog
    breakpoint_id: responsive_image.viewport_sizing
    multiplier: 1x
breakpoint_group: responsive_image
fallback_image_style: catalog
