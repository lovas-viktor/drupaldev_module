services:
  theme.negotiator.role_based_theme_switcher:
    class: Drupal\drupaldev\Theme\RoleNegotiator
    arguments: [ '@current_user', '@config.factory', '@entity_type.manager', '@router.admin_context' ]
    tags:
      - { name: theme_negotiator, priority: -30 }
